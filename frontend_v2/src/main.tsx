import React from 'react';
import ReactDOM from 'react-dom/client';
import ConceptBridgeApp from './ConceptBridgeApp';
import './index.css'; // Tailwind

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ConceptBridgeApp />
  </React.StrictMode>
)
